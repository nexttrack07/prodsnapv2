node_modules
package-lock.json
yarn.lock

.DS_Store
.cache
.env
.vercel
.output
/build/
/api/
/server/build
/public/build# Sentry Config File
.env.sentry-build-plugin

.env.local

# Build artifacts
dist/

# Playwright
playwright-report/
test-results/

# Local tooling scratch / caches — not for the repo
.firecrawl/
.netlify/

# Claude Code agent runtime — only the Convex AI skill docs are tracked
.claude/
!.claude/skills/
!.claude/skills/**

# OMC runtime state — only runbooks are tracked
.omc/
!.omc/runbooks/
!.omc/runbooks/**

# OMC skills lockfile — regenerated on every run
skills-lock.json

# TanStack Start build cache
.tanstack/
